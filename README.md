## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

[pages/api/hello.ts](./pages/api/hello.ts) gives an example of how you can deploy an API routes as an Edge Function on Vercel.
